export enum Keys {
    ENTER = 'Enter',
    ESCAPE = 'Escape',
    UP = 'ArrowUp',
    DOWN = 'ArrowDown',
}

export enum Navigator {
    Waze = 'waze',
    Google = 'google',
    Yandex = 'yandex',
    Apple = 'apple',
}

export enum SearchType {
    DD = 'DD',
    DMS = 'DMS',
    TEXT = 'TEXT',
}
